# Keystore Configuration for Android App Signing
# Copy these environment variables to your .env file

# Path to your keystore file
KEYSTORE_PATH=C:\Users\T O M A\OneDrive\Documents\my-app-release-key

# Keystore alias (the name you gave your key)
KEYSTORE_ALIAS=your-keystore-alias

# Keystore password
KEYSTORE_PASSWORD=your-keystore-password

# Key password (might be the same as keystore password)
KEY_PASSWORD=your-key-password

# Note: Replace the placeholder values above with your actual keystore details
# Keep this file secure and never commit it to version control 